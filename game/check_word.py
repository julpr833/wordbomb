def word_exists(word: str) -> bool:
    
    """Determina si una palabra existe o no en el idioma español.
    
    Toma una palabra de entrada y devuelve True si la palabra existe en el idioma
    español, False en caso contrario.
    
    PARAMS
    :word <str>: La palabra de entrada
    
    OUTPUT
    :<bool>: True si la palabra existe, False en caso contrario
    """
    pass
